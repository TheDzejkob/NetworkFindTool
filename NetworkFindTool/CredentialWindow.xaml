<Window x:Class="NetworkFindTool.CredentialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Switch Credentials" Height="250" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Background="#232946" Icon="icon.ico">
    <Window.Resources>
        <Style x:Key="PlaceholderTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            <TextBlock Text="{TemplateBinding Tag}" 
                               Foreground="#888" 
                               FontStyle="Italic" 
                               Margin="5,0,0,0"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"
                               Visibility="{Binding Text.IsEmpty, 
                                    RelativeSource={RelativeSource TemplatedParent}, 
                                    Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RoundedPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="15" Background="#232946">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Username:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="#fffffe" FontWeight="Bold" FontSize="16" />
        <TextBox x:Name="UsernameInput" Grid.Row="0" Grid.Column="1" Margin="5" MinWidth="150" VerticalContentAlignment="Center" Background="#b8c1ec" Foreground="#232946" FontWeight="DemiBold">
            <TextBox.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </TextBox.Resources>
        </TextBox>

        <Label Content="Password:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="#fffffe" FontWeight="Bold" FontSize="16"/>
        <PasswordBox x:Name="PasswordInput" Grid.Row="1" Grid.Column="1" Margin="5" MinWidth="150" VerticalContentAlignment="Center" Background="#b8c1ec" Foreground="#232946" FontWeight="DemiBold" Style="{StaticResource RoundedPasswordBoxStyle}"/>

        <CheckBox x:Name="RememberCredentialsCheckBox"
                   BorderBrush="#121629"
                   Background="#eebbc3"
                   Grid.Row="2" Grid.ColumnSpan="2"
                   Content="Remember credentials"
                   Margin="0,10,0,10"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Foreground="#fffffe"
                   FontWeight="Bold"
                   FontSize="16"
                   Height="32"
                   Width="220"
                   Padding="8,0,0,0"
                   HorizontalContentAlignment="Center"
                   VerticalContentAlignment="Center">
            <CheckBox.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </CheckBox.Resources>
        </CheckBox>

        <Button Grid.Row="3" Grid.ColumnSpan="2" Content="Submit" Width="100" Height="35" HorizontalAlignment="Center" Click="OkButton_Click" Background="#eebbc3" Foreground="#232946" FontSize="18" FontWeight="Bold">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
